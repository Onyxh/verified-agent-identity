with:
  name: Onyxh
  description: Onyxh AI Agent
name: Verify Agent Identity

on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Verify Identity
        uses: BillionsNetwork/verified-agent-identity/.github/actions/verify@main
        with:
          name: Onyxh
          description: Onyxh AI Agent
